extends layout

block content
  //- if message
  //-   .row
  //-     .small-12.columns
  //-       .alert-box.alert(data-alert='data-alert')
  //-         p #{message}
  //-         a.close(href='#') &times;
  form(action="/login", method="post")
    .row
      .small-12.columns
        h3 Log In
    .row
      .small-6.columns
        input(type="text", name="username", placeholder="Username")
        input(type="password", name="password", placeholder="Password")
    .row
      .small-12.columns
        button(type="Submit") Log In